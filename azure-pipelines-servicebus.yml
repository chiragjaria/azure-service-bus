trigger:
- main

pool:
  vmImage: ubuntu-latest

variables:
  terraformVersion: '1.7.0'
  workingDir: '$(System.DefaultWorkingDirectory)/terraform'

  # Azure
  serviceConnection: 'azure-service-connection'
  resourceGroup: 'rgp-dev-mes'
  location: 'Central India'

  # Service Bus
  serviceBusNamespace: 'sb-mes-dev-cj'
  queueName: 'orders-queue'

  # Backend
  backendStorageAccount: 'tddevtfstate123'
  backendContainer: 'tfstate'
  backendKey: 'servicebus.terraform.tfstate'

stages:

# ================= TERRAFORM =================
- stage: TerraformDeploy
  displayName: Provision Service Bus Infra
  jobs:
  - job: TerraformJob
    displayName: Terraform Service Bus
    steps:

    # Install Terraform
    - task: TerraformInstaller@1
      displayName: Install Terraform
      inputs:
        terraformVersion: $(terraformVersion)

    # Terraform Init
    - task: TerraformTaskV4@4
      displayName: Terraform Init
      inputs:
        provider: azurerm
        command: init
        workingDirectory: $(workingDir)
        backendServiceArm: $(serviceConnection)
        backendAzureRmResourceGroupName: $(resourceGroup)
        backendAzureRmStorageAccountName: $(backendStorageAccount)
        backendAzureRmContainerName: $(backendContainer)
        backendAzureRmKey: $(backendKey)

    # Terraform Plan
    - task: TerraformTaskV4@4
      displayName: Terraform Plan
      inputs:
        provider: azurerm
        command: plan
        workingDirectory: $(workingDir)
        environmentServiceNameAzureRM: $(serviceConnection)
        commandOptions: |
          -var="resource_group_name=$(resourceGroup)"
          -var="location=$(location)"
          -var="servicebus_namespace_name=$(serviceBusNamespace)"
          -var="queue_name=$(queueName)"

    # Terraform Apply
    - task: TerraformTaskV4@4
      displayName: Terraform Apply
      inputs:
        provider: azurerm
        command: apply
        workingDirectory: $(workingDir)
        environmentServiceNameAzureRM: $(serviceConnection)
        commandOptions: |
          -var="resource_group_name=$(resourceGroup)"
          -var="location=$(location)"
          -var="servicebus_namespace_name=$(serviceBusNamespace)"
          -var="queue_name=$(queueName)"
          -auto-approve

# ================= VERIFY =================
- stage: Verify
  displayName: Verify Service Bus
  dependsOn: TerraformDeploy
  jobs:
  - job: VerifyJob
    steps:

    - task: AzureCLI@2
      displayName: Verify Service Bus Namespace
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "Checking Service Bus Namespace..."

          az servicebus namespace show \
            --name $(serviceBusNamespace) \
            --resource-group $(resourceGroup)

          echo "Checking Queue..."

          az servicebus queue show \
            --name $(queueName) \
            --namespace-name $(serviceBusNamespace) \
            --resource-group $(resourceGroup)

          echo "✅ Service Bus verification completed"
