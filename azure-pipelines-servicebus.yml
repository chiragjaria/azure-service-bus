trigger:
- main

pool:
  vmImage: ubuntu-latest

variables:
  terraformVersion: '1.7.0'
  workingDir: '$(System.DefaultWorkingDirectory)/terraform'
  # Azure
  serviceConnection: 'azure-service-connection'
  resourceGroup: 'rgp-dev-mes'
  location: 'Central India'
  # Service Bus
  serviceBusNamespace: 'sb-mes-dev-cj'
  queueName: 'orders-queue'
  # Backend
  backendStorageAccount: 'tddevtfstate123'
  backendContainer: 'tfstate'
  backendKey: 'servicebus.terraform.tfstate'
  # Function App
  functionAppName: 'func-sb-dev-cj'
  functionAppPath: '$(System.DefaultWorkingDirectory)'   # ✅ repo root IS the function app root
  keyVaultName: 'kvmesdev123'
  pythonVersion: '3.10'

stages:

# ═══════════════════════════════════════════════════════════════
# STAGE 1: TERRAFORM - Infrastructure Provisioning
# ═══════════════════════════════════════════════════════════════
- stage: Stage1_TerraformDeploy
  displayName: '1️⃣ Terraform - Service Bus Infrastructure'
  jobs:
  - job: TerraformJob
    displayName: Terraform Service Bus
    steps:

    - checkout: self
      displayName: 'Checkout Repository'

    - task: TerraformInstaller@1
      displayName: Install Terraform $(terraformVersion)
      inputs:
        terraformVersion: $(terraformVersion)

    - task: TerraformTaskV4@4
      displayName: 'Terraform Init - Initialize Backend'
      inputs:
        provider: azurerm
        command: init
        workingDirectory: $(workingDir)
        backendServiceArm: $(serviceConnection)
        backendAzureRmResourceGroupName: $(resourceGroup)
        backendAzureRmStorageAccountName: $(backendStorageAccount)
        backendAzureRmContainerName: $(backendContainer)
        backendAzureRmKey: $(backendKey)

    - task: TerraformTaskV4@4
      displayName: 'Terraform Validate - Check Syntax'
      inputs:
        provider: azurerm
        command: validate
        workingDirectory: $(workingDir)

    - task: TerraformTaskV4@4
      displayName: 'Terraform Plan - Show Changes'
      inputs:
        provider: azurerm
        command: plan
        workingDirectory: $(workingDir)
        environmentServiceNameAzureRM: $(serviceConnection)

    - task: TerraformTaskV4@4
      displayName: 'Terraform Apply - Create Resources'
      inputs:
        provider: azurerm
        command: apply
        workingDirectory: $(workingDir)
        environmentServiceNameAzureRM: $(serviceConnection)
        commandOptions: '-auto-approve'

    - task: AzureCLI@2
      displayName: 'Display Terraform Outputs'
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        workingDirectory: $(workingDir)
        inlineScript: |
          echo "════════════════════════════════════════════"
          echo "✅ Terraform Outputs"
          echo "════════════════════════════════════════════"
          terraform output -no-color
          echo ""
          echo "Service Bus Namespace: $(serviceBusNamespace)"
          echo "Queue Name: $(queueName)"
          echo "Function App: $(functionAppName)"

# ═══════════════════════════════════════════════════════════════
# STAGE 2: VERIFY - Service Bus Verification
# ═══════════════════════════════════════════════════════════════
- stage: Stage2_Verify
  displayName: '2️⃣ Verify - Service Bus Configuration'
  dependsOn: Stage1_TerraformDeploy
  condition: succeeded()
  jobs:

  - job: VerifyServiceBus
    displayName: Verify Service Bus Resources
    steps:

    - checkout: self
      displayName: 'Checkout Repository'

    - task: AzureCLI@2
      displayName: 'Verify Service Bus Namespace'
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "═════════════════════════════════════════════"
          echo "🔍 Checking Service Bus Namespace"
          echo "═════════════════════════════════════════════"
          az servicebus namespace show \
            --name $(serviceBusNamespace) \
            --resource-group $(resourceGroup) \
            --query '{Name:name, Status:provisioningState, Sku:sku.name}' \
            -o table
          echo "✅ Service Bus Namespace verified"

    - task: AzureCLI@2
      displayName: 'Verify Service Bus Queue'
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "═════════════════════════════════════════════"
          echo "🔍 Checking Queue: $(queueName)"
          echo "═════════════════════════════════════════════"
          az servicebus queue show \
            --name $(queueName) \
            --namespace-name $(serviceBusNamespace) \
            --resource-group $(resourceGroup) \
            --query '{Name:name, MaxDeliveryCount:maxDeliveryCount, LockDuration:lockDuration, DefaultMessageTTL:defaultMessageTtl}' \
            -o table
          echo "✅ Queue verified"

    - task: AzureCLI@2
      displayName: 'Verify Function App'
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "═════════════════════════════════════════════"
          echo "🔍 Checking Function App: $(functionAppName)"
          echo "═════════════════════════════════════════════"
          az functionapp show \
            --name $(functionAppName) \
            --resource-group $(resourceGroup) \
            --query '{Name:name, State:state, DefaultHostName:defaultHostName}' \
            -o table
          echo "✅ Function App verified"

    - task: AzureCLI@2
      displayName: 'Verify Key Vault Secrets'
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "═════════════════════════════════════════════"
          echo "🔍 Checking Key Vault Secrets"
          echo "═════════════════════════════════════════════"
          echo "Listed secrets in $(keyVaultName):"
          az keyvault secret list \
            --vault-name $(keyVaultName) \
            --query '[].name' \
            -o table
          echo "✅ Key Vault verified"

# ═══════════════════════════════════════════════════════════════
# STAGE 3: CODE BUILD & DEPLOY - Function App Deployment
# ═══════════════════════════════════════════════════════════════
- stage: Stage3_CodeBuildDeploy
  displayName: '3️⃣ Code Build & Deploy - Function App'
  dependsOn: Stage2_Verify
  condition: succeeded()
  jobs:

  - job: BuildAndDeployFunctionApp
    displayName: Build and Deploy Function App
    steps:

    - checkout: self
      displayName: 'Checkout Repository'

    - task: UsePythonVersion@0
      displayName: 'Setup Python $(pythonVersion)'
      inputs:
        versionSpec: '$(pythonVersion)'
        addToPath: true

    # ✅ Validate structure first (v2 model - single root function_app.py)
    - task: Bash@3
      displayName: 'Validate Function Structure'
      inputs:
        targetType: 'inline'
        script: |
          echo "═════════════════════════════════════════════"
          echo "✓ Validating Function Structure"
          echo "═════════════════════════════════════════════"
          cd $(functionAppPath)

          echo "Workspace contents:"
          ls -la

          if [ -f "function_app.py" ]; then
            echo "✅ function_app.py: Present"
          else
            echo "❌ function_app.py: Missing - make sure it is at the repo root"
            exit 1
          fi

          if [ -f "host.json" ]; then
            echo "✅ host.json: Present"
          else
            echo "❌ host.json: Missing"
            exit 1
          fi

          if [ -f "requirements.txt" ]; then
            echo "✅ requirements.txt: Present"
          else
            echo "❌ requirements.txt: Missing"
            exit 1
          fi

          if [ -f "OrderSender/function.json" ] || [ -f "OrderProcessor/function.json" ]; then
            echo "❌ Found function.json - delete it! v2 model uses decorators only."
            exit 1
          else
            echo "✅ No conflicting function.json files"
          fi

          echo ""
          echo "✅ All structure checks passed"

    # ✅ Code syntax check
    - task: Bash@3
      displayName: 'Code Quality Check'
      inputs:
        targetType: 'inline'
        script: |
          echo "═════════════════════════════════════════════"
          echo "🔍 Code Quality Check"
          echo "═════════════════════════════════════════════"
          cd $(functionAppPath)
          python -m py_compile function_app.py
          echo "✅ Code syntax check passed"

    # ✅ FIXED: Build zip package with dependencies installed inside it
    # This is required for Linux Consumption/Premium plans
    - task: Bash@3
      displayName: 'Build Deployment Package (ZIP with dependencies)'
      inputs:
        targetType: 'inline'
        script: |
          echo "═════════════════════════════════════════════"
          echo "📦 Building Deployment Package"
          echo "═════════════════════════════════════════════"

          DEPLOY_DIR="$(Build.ArtifactStagingDirectory)/function-app"
          mkdir -p "$DEPLOY_DIR"

          # Copy function app files
          cp $(functionAppPath)/function_app.py "$DEPLOY_DIR/"
          cp $(functionAppPath)/host.json "$DEPLOY_DIR/"
          cp $(functionAppPath)/requirements.txt "$DEPLOY_DIR/"

          # ✅ Install dependencies INTO the package folder
          pip install \
            --target "$DEPLOY_DIR/.python_packages/lib/site-packages" \
            --no-cache-dir \
            -r $(functionAppPath)/requirements.txt

          echo ""
          echo "Package contents:"
          ls -la "$DEPLOY_DIR/"
          echo ""
          echo "✅ Deployment package built with dependencies"

    # ✅ Deploy using zip deploy method (most reliable for Linux)
    - task: AzureFunctionApp@2
      displayName: 'Deploy to Azure Function App'
      inputs:
        connectedServiceNameARM: $(serviceConnection)
        appType: 'functionAppLinux'
        appName: $(functionAppName)
        package: '$(Build.ArtifactStagingDirectory)/function-app'
        runtimeStack: 'PYTHON|$(pythonVersion)'
        deploymentMethod: 'zipDeploy'

    # Post-Deployment Verification
    - task: AzureCLI@2
      displayName: 'Post-Deployment Verification'
      inputs:
        azureSubscription: $(serviceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "═════════════════════════════════════════════"
          echo "✅ POST-DEPLOYMENT VERIFICATION"
          echo "═════════════════════════════════════════════"

          echo "Checking Function App state..."
          az functionapp show \
            --name $(functionAppName) \
            --resource-group $(resourceGroup) \
            --query '{Name:name, State:state, DefaultHostName:defaultHostName}' \
            -o table

          echo ""
          echo "Checking registered functions..."
          az functionapp function list \
            --name $(functionAppName) \
            --resource-group $(resourceGroup) \
            --query '[].{Name:name, Language:language}' \
            -o table

          echo ""
          echo "═════════════════════════════════════════════"
          echo "Function App URL:"
          echo "https://$(functionAppName).azurewebsites.net"
          echo "═════════════════════════════════════════════"
          echo ""
          echo "✅ Deployment completed!"

    # Test endpoint
    - task: Bash@3
      displayName: 'Test OrderSender Endpoint'
      inputs:
        targetType: 'inline'
        script: |
          echo "═════════════════════════════════════════════"
          echo "🧪 Testing OrderSender Endpoint"
          echo "═════════════════════════════════════════════"

          # Wait for function app to fully start
          echo "Waiting 30 seconds for function app to initialize..."
          sleep 30

          FUNCTION_URL="https://$(functionAppName).azurewebsites.net/api/orders"
          echo "Sending test request to: $FUNCTION_URL"
          echo ""

          RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" -X POST "$FUNCTION_URL" \
            -H "Content-Type: application/json" \
            -d '{
              "order_id": 1,
              "customer_name": "Test Customer",
              "items": ["item1", "item2"],
              "total_amount": 100
            }')

          HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | grep -v "HTTP_STATUS")

          echo "HTTP Status: $HTTP_STATUS"
          echo "Response Body:"
          echo "$BODY" | jq . 2>/dev/null || echo "$BODY"

# ═══════════════════════════════════════════════════════════════
# STAGE 4: SUMMARY
# ═══════════════════════════════════════════════════════════════
- stage: Stage4_Summary
  displayName: '✅ Summary'
  dependsOn:
    - Stage1_TerraformDeploy
    - Stage2_Verify
    - Stage3_CodeBuildDeploy
  condition: succeeded()
  jobs:
  - job: SuccessJob
    displayName: Deployment Summary
    steps:

    - task: Bash@3
      displayName: 'Deployment Success Summary'
      inputs:
        targetType: 'inline'
        script: |
          echo ""
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║        🎉 DEPLOYMENT COMPLETED SUCCESSFULLY 🎉            ║"
          echo "╚════════════════════════════════════════════════════════════╝"
          echo ""
          echo "📊 What was deployed:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Service Bus Namespace: $(serviceBusNamespace)"
          echo "✅ Queue: $(queueName)"
          echo "✅ Function App: $(functionAppName)"
          echo "✅ OrderSender Function (HTTP Trigger)"
          echo "✅ OrderProcessor Function (Queue Trigger)"
          echo ""
          echo "🧪 Test command:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "curl -X POST https://$(functionAppName).azurewebsites.net/api/orders \\"
          echo "  -H 'Content-Type: application/json' \\"
          echo "  -d '{\"order_id\": 1, \"customer_name\": \"John\", \"items\": [\"pizza\"]}'"
          echo ""
